/**
 * Liveblocks client configuration.
 *
 * In the current setup, the client is configured via <LiveblocksProvider>
 * with `authEndpoint="/api/liveblocks-auth"`. This file exists as a
 * central place for any future client-level configuration overrides.
 */
export const LIVEBLOCKS_AUTH_ENDPOINT = "/api/liveblocks-auth";
